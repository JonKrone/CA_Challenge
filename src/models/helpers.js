// This helper provides a more concise method of flushing and
// extracting JSON data from a fetch response
export function json(response) {return response.json();}

// temp access token
export const headers = {
  // Authorization: ''
};

export const API = 'https://api.github.com';
